<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.1" xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd">
    <persistence-unit name="ceiba-estacionamiento" transaction-type="RESOURCE_LOCAL">

        <class>co.com.ceiba.persistencia.entitad.LibroEntity</class>
        <class>co.com.ceiba.persistencia.entitad.PrestamoEntity</class>

        <properties>
            <!-- Configuring JDBC properties -->
            
            <!-- <property name="javax.persistence.jdbc.url" value="jdbc:h2:mem:test"/> -->
            <property name="javax.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="connection.url" value="jdbc:h2:database/ceiba-estacionamiento"/>
	        <property name="connection.username" value="sa" />
	        <property name="connection.password" value=""/>
	        
	        <!-- JDBC connection pool (use the built-in) -->
        	<property name="connection.pool_size" value="1"/>

            <!-- Hibernate properties -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>
            <property name="hibernate.hbm2ddl.auto" value="create"/>
            <property name="hibernate.show_sql" value="true"/>

        </properties>
    </persistence-unit>
</persistence>